apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml7ma-backend
  labels:
    app: ml7ma-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml7ma-backend
  template:
    metadata:
      labels:
        app: ml7ma-backend
    spec:
      initContainers:
        - name: run-migrations
          image: eladwy/backend:005c0443a094dbc6bd2dcb6fe5aac0a8e41c3b2d
          command: ["sh", "-c"]
          args:
            - |
              python manage.py migrate --no-input && \
              python manage.py createcachetable
          envFrom:
            - secretRef:
                name: mysql-secret
      containers:
        - name: ml7ma-backend
          image: eladwy/backend:005c0443a094dbc6bd2dcb6fe5aac0a8e41c3b2d
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: mysql-secret
                
