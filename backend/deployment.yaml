apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml7ma-backend
  labels:
    app: ml7ma-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml7ma-backend
  template:
    metadata:
      labels:
        app: ml7ma-backend
    spec:
      initContainers:
        - name: run-migrations
          image: eladwy/backend:9de3b5c7111f3e0c0396f941d1ba0ed77b8c317f
          command: ["sh", "-c"]
          args:
            - |
              python manage.py migrate --no-input && \
              python manage.py createcachetable
          envFrom:
            - secretRef:
                name: mysql-secret
      containers:
        - name: ml7ma-backend
          image: eladwy/backend:9de3b5c7111f3e0c0396f941d1ba0ed77b8c317f
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: mysql-secret
                
